<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Pixel Pink World</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Pixel font -->
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">

  <style>
    :root {
      --pink-light: #ffd6e8;
      --pink-main: #ff8fc7;
      --pink-dark: #e7549f;
      --pixel-border: #b23a7c;
          --bg-dark: #2b0f1f;


    * {
      box-sizing: border-box;
      image-rendering: pixelated;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: "Press Start 2P", monospace;
      background:
        linear-gradient(90deg, rgba(255,255,255,0.05) 1px, transparent 1px),
        linear-gradient(rgba(255,255,255,0.05) 1px, transparent 1px),
        var(--bg-dark);
      background-size: 24px 24px;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .game-window {
      background: var(--pink-light);
      color: #3a0d25;
      width: 90%;
      max-width: 520px;
      padding: 24px;
      border: 6px solid var(--pixel-border);
      box-shadow:
        0 0 0 4px var(--pink-main),
        8px 8px 0 #000;
      text-align: center;
    }

    h1 {
      font-size: 1.1rem;
      margin-bottom: 16px;
      color: var(--pink-dark);
    }

    .pixel-box {
      background: #fff;
      padding: 16px;
      border: 4px solid var(--pixel-border);
      margin-bottom: 20px;
      font-size: 0.6rem;
      line-height: 1.6;
    }

    .button-row {
      display: flex;
      gap: 12px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .pixel-btn {
      background: var(--pink-main);
      color: #fff;
      border: 4px solid var(--pixel-border);
      padding: 12px 16px;
      font-family: inherit;
      font-size: 0.6rem;
      cursor: pointer;
      box-shadow: 4px 4px 0 #000;
      transition: transform 0.05s ease, box-shadow 0.05s ease;
    }

    .pixel-btn:hover {
      background: var(--pink-dark);
    }

    .pixel-btn:active {
      transform: translate(4px, 4px);
      box-shadow: 0 0 0 #000;
    }

    .footer {
      margin-top: 16px;
      font-size: 0.45rem;
      opacity: 0.7;
    }

    /* Cute floating hearts */
    .heart {
      position: fixed;
      color: var(--pink-main);
      font-size: 12px;
      animation: floatUp 3s linear forwards;
      pointer-events: none;
    }

    @keyframes floatUp {
      from {
        transform: translateY(0);
        opacity: 1;
      }
      to {
        transform: translateY(-80px);
        opacity: 0;
      }
    }
  </style>
</head>

<body>
  <div class="game-window">
    <h1>         <img src='https://i.postimg.cc/bDYFDC4G/mamegoma-gif.gif' border='0' alt='mamegoma-gif' class="valentine-gif">
 </h1>
    <div class="pixel-box">
      YOU'RE MY FAVORITE PLAYER 2.<br>
      WILL YOU BE MY VALENTINE?<br>
    </div>

    <div class="button-row">
      <button class="pixel-btn" onclick="spawnHeart()">START</button>
      <button class="pixel-btn" onclick="spawnHeart()">OPTIONS</button>
      <button class="pixel-btn" onclick="spawnHeart()">EXIT</button>
    </div>

    <div class="footer">
      press start to feel cute
    </div>
  </div>

  <script>
    function spawnHeart() {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.textContent = "♥";
      heart.style.left = Math.random() * window.innerWidth + "px";
      heart.style.top = window.innerHeight - 40 + "px";
      document.body.appendChild(heart);

      setTimeout(() => heart.remove(), 3000);
    }
  </script>
</body>
</html>